

# Increase Hero Image Size in Booking Drawer

## What changes
Increase the hero image from `h-[40vh]` to `h-[55vh]` so more of the service image is visible, pushing the booking flow content further down.

## Technical Details

**File: `src/components/booking/BookingDrawer.tsx`**
- Change the hero image container from `h-[40vh]` to `h-[55vh]`

